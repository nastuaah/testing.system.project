CREATE TABLE ANSWERS (
    ID          NUMERIC (10) PRIMARY KEY,
    ANSWER      TEXT (1000),
    IS_RIGHT    INTEGER (1),
    QUESTION_ID NUMERIC (10) REFERENCES QUESTIONS (ID),
    NUMBER      INTEGER (1) 
);
